# dependencies
node_modules/

# builds
dist/
.next/
out/
contracts/build/

# env — never commit secrets (Blockfrost keys etc.)
.env
.env.local
.env.*.local

# runtime settings (may hold a Blockfrost/Koios key set via the app's
# Settings page instead of .env) — never commit
backend/data/

# misc
*.log
.DS_Store
coverage/

# throwaway preprod e2e test wallets — never commit
e2e/wallets/
