# Builds and publishes production Docker images to GHCR when a version tag
# is pushed (git tag v0.1.0 && git push --tags). Images:
#   ghcr.io/<owner>/<repo>-backend:<version|latest>
#   ghcr.io/<owner>/<repo>-frontend:<version|latest>
#
# Note: the frontend image inlines NEXT_PUBLIC_API_URL=http://localhost:3001
# at build time, so the prebuilt image is for same-machine use (which is the
# intended self-hosted setup). Hosting under another name? Build locally:
#   docker compose build --build-arg NEXT_PUBLIC_API_URL=http://your-host:3001 frontend
name: release-images

on:
  push:
    tags: ["v*"]

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [backend, frontend]
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}-${{ matrix.service }}
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,scope=${{ matrix.service }},mode=max
