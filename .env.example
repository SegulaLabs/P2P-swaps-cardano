# Root env — consumed by infra/docker-compose.yml
# Copy to .env and adjust. Never commit the real .env.

POSTGRES_USER=p2pdex
POSTGRES_PASSWORD=change-me-locally
POSTGRES_DB=p2pdex
POSTGRES_PORT=5432
